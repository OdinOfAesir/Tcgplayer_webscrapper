# API Key for authentication
# Generate a strong random password and set it here
# Example: openssl rand -base64 32
API_KEY=your-strong-random-password-here

# Network/resource tuning
# Set to 0 to allow images when scraping (defaults to blocking image requests)
BLOCK_IMAGES=1
